import Container from 'react-bootstrap/Container';
import Navbar from 'react-bootstrap/Navbar'
import '../App.css';
import logo from '../assets/img/logo.png';

const LogoGatos = () => {
    return(
        <img className="logo_header  " src={logo}>
            </img>
    );
};
export default LogoGatos;